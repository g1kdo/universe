rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules - more permissive for testing
    match /{document=**} {
      allow read, write: if true; // Allow all read/write for development
    }
  }
}